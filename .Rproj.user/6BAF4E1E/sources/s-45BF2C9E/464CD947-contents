library(shinyjs)

add_data_page <- shinyUI (fluidPage(
  mainPanel (h3("Upload Data"),
             # htmlOutput("uploadpage"),
             p ("This functionality is still under construction"),

             

               
               
               
               fileInput("file1", "Choose CSV File",
                         multiple = F,
                         accept = c("text/csv",
                                    "text/comma-separated-values,text/plain",
                                    ".csv")))
  
  
  
))